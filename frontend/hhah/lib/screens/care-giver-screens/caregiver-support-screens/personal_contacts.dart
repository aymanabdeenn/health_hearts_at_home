import 'package:flutter/material.dart';

class PersonalContacts extends StatelessWidget {
  const PersonalContacts({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
